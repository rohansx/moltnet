# binaries
/bin/
/moltnetd
/molt
*.exe

# local registry data
*.db
*.db-wal
*.db-shm

# keys — never commit private key material
*.key

# generated cards during local testing
card.json
card*.json
issuer-card.json
issuer.key

# frontend (Vite) build artifacts + deps
frontend/node_modules/
frontend/dist/
*.tsbuildinfo

# OS / editor
.DS_Store
*.swp
